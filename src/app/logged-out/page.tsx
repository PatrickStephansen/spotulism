import { LoginButton } from "../_components/login-button";

export default function LoggedOut(){
  return <p>You must log in to Spotify and grant permissions for the app to play anything. <LoginButton/></p>
}
